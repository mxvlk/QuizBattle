const indexPath = "/";
const allPath = "*";

const loginPath = "/login";
const registerPath = "/register";

const gameStatePath = "/gs";
const questionPath = "/q";
const mainPagePath = "/m";
const categoryPath = "/c";

export {
    allPath,
    indexPath,
    loginPath,
    registerPath,
    gameStatePath,
    questionPath,
    mainPagePath,
    categoryPath
}